
        <h2 class="welcome">
          <span>Recent</span>
        </h2>
        <ul id="recentCoupons">
        {{#data}}
			<li>
				<p>{{name}} <span class="triangle">${{price}}</span></p>
				<p><img src="{{logo_url}}"/></p>
				<p>Brief: {{description}}</p>
			</li>
		{{/data}}
        </ul>
        
    

